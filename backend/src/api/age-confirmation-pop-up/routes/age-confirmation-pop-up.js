'use strict';

/**
 * age-confirmation-pop-up router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::age-confirmation-pop-up.age-confirmation-pop-up');
